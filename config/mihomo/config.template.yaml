# mihomo runtime configuration template for the NUC.
#
# The render script (config/mihomo/render.py) substitutes these markers and
# writes the result to the state directory with mode 0600:
#   __CONTROLLER_SECRET__       controller/dashboard bearer secret
#   __CONTROLLER_ENDPOINT__     controller/dashboard bind address
#   # __PROXY_PROVIDERS__      replaced by the subscription provider block
#   # __CUSTOM_PROXIES__       replaced by the runtime custom proxies
#   # __PROXY_GROUPS__         replaced by the rules-override proxy-groups
#   # __RULES__                replaced by the rules-override rules
#
# The subscription only carries proxy nodes; proxy groups, rule-providers and
# rules come from an external Clash override document fetched by render.py.
#
# Never put a subscription URL, node address, or node credential in this file:
# those live only in the runtime secret files and the rendered state directory.

mixed-port: 7890
mode: rule
log-level: info
ipv6: false
allow-lan: true
find-process-mode: off
unified-delay: true
tcp-concurrent: true

external-controller: "__CONTROLLER_ENDPOINT__"
secret: "__CONTROLLER_SECRET__"

profile:
  store-selected: true
  store-fake-ip: true

# Keep the GEO databases fresh from a China-friendly mirror. Rule matching
# falls back to the last successful download when the mirror is unreachable.
geodata-mode: true
geodata-loader: standard
geo-auto-update: true
geo-update-interval: 24
geox-url:
  geoip: "https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geoip.dat"
  geosite: "https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geosite.dat"
  mmdb: "https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/country.mmdb"
  asn: "https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/GeoLite2-ASN.mmdb"

dns:
  enable: true
  ipv6: false
  # default-nameserver must contain IP literals; it resolves the DNS servers.
  default-nameserver:
    - 223.5.5.5
    - 119.29.29.29
  # Resolve everything through China-friendly DoH. A fallback list of
  # 1.1.1.1 / dns.google is unreachable here and makes rules and providers on
  # non-CN CDNs fail to resolve, so resolution stays on the domestic servers.
  nameserver:
    - https://doh.pub/dns-query
    - https://dns.alidns.com/dns-query
  proxy-server-nameserver:
    - https://doh.pub/dns-query

# __PROXY_PROVIDERS__

# Custom nodes are declared at the top level (not as a provider) so that
# dialer-proxy chains between them resolve; the override's include-all groups
# still pick them up.
# __CUSTOM_PROXIES__

# Proxy groups, rule-providers and rules come from the external override
# document. render.py splices them into the placeholders below.
# __PROXY_GROUPS__

# __RULES__
